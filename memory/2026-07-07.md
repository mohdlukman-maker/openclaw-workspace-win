- Updated `invoice_extractor` so `/editnothensave` lets a user choose the P.O running number before saving a pending D.O + invoice pair. The command accepts `/editnothensave 7` or prompts for the next text message if no number is supplied, rejects already-used monthly numbers, and keeps `/save` as automatic numbering. Verified helper tests and compile passed, then restarted `Invoice Extractor Bot`; Telegram polling started successfully.
- Repaired `invoice_extractor` variable-output issue: rejected bad thin perspective crops, made TUJU local OCR fallback less brittle, tightened AI fallback prompts to return null instead of guessing unclear document/date/contact fields, and added same-source-image extraction caching so identical images reuse the same result. Verified py_compile, 25 helper tests, targeted bad-crop sample, and restarted `Invoice Extractor Bot` successfully.
- Switched `invoice_extractor` to AI-primary extraction at Lukman's request. Added `AI_PRIMARY_ENABLED=1`, AI full-page + focused-crop image packaging, local OCR as verification only, and post-validation that rejects invalid document numbers such as `TIC-X08941` and normalizes visible dates like `01.07.2026` to `2026-07-01`. Verified py_compile, 28 helper tests, a targeted non-network sample check, and restarted `Invoice Extractor Bot` successfully.
