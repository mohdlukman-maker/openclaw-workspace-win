# 2026-06-27

- Investigated `invoice_extractor` after Lukman said the invoice Telegram bot was not responding. Root cause: no Python invoice bot process was running, and the shared `receipt_extractor\.venv` was missing invoice OCR dependencies (`pillow`, `pytesseract`).
- Installed `invoice_extractor\requirements.txt` into `receipt_extractor\.venv`, verified `invoice_bot.py` compiles, and started the bot successfully.
- Added a Windows scheduled task named `Invoice Extractor Bot` that runs `invoice_extractor\invoice_bot.py` at logon using `receipt_extractor\.venv`, starts when available, runs on battery, has no execution time limit, ignores duplicate instances, and restarts every minute up to 999 times.
- Changed invoice Excel output per Lukman's request: each approved invoice now saves into its own workbook under `invoice_extractor\data\invoices\`, with the filename built from the extracted Tax Invoice / invoice number. Updated `.env`, `.env.example`, and `README.md`; compile and sample workbook-save tests passed; restarted the scheduled bot successfully.
- Fixed invoice bot failure where weak local OCR triggered OpenAI fallback and an invalid OpenAI API key caused a Telegram authentication error. `extract_invoice_hybrid` now returns the best local OCR result when AI fallback fails due to OpenAI authentication, with a warning to review carefully before saving. Verified using the latest saved invoice image and restarted the scheduled bot.
- Resolved the remaining OpenAI auth warning by validating the `.env` OpenAI key directly, changing `load_dotenv(BASE_DIR / ".env")` to `override=True` so the bot's validated config wins over stale Windows environment variables, then restarting `Invoice Extractor Bot`. Direct AI extraction on the latest invoice image succeeded (`TG-R3857`, 13 line items).
- Fixed the invoice bot review display issue where Telegram timed out while sending the extraction summary, causing the handler to stop before sending the extracted item rows. Added `safe_reply_text` with longer Telegram timeouts and timeout handling, changed `reply_long_text` and review-summary/instruction sends to continue after send timeouts, compiled successfully, and restarted the scheduled bot.
- Fixed intermittent missed invoice rows: OCR showed `TG-R3857` had visible item rows 1-14 while first AI output sometimes returned only 13 or shifted rows 10-14. Added OCR row-count validation, an AI reconciliation pass when visible row numbers exceed extracted rows, stronger table reread instructions, and arithmetic repair for quantities with missing digits. Direct test on the latest saved invoice returned 14 corrected rows; compiled and restarted `Invoice Extractor Bot`.
- Updated the invoice bot to use Lukman's uploaded purchase-order Excel template (`invoice_extractor\data\templates\purchase_order_template.xlsx`) when saving approved invoices. Template mapping: sheet `PURCHASEORDER`, date `J16`, invoice number `J17`, item rows `26:54`, columns `B/C/G/H/J` for item/description/quantity/unit price/amount. Regenerated latest extracted invoice `TG-R3922.xlsx` into the template, attached it back in Telegram, compiled successfully, and restarted `Invoice Extractor Bot`.
- Added PDF export to the invoice bot. After `/save`, the bot now saves the filled purchase-order Excel workbook and exports a matching `<Tax-Invoice>.pdf` using Windows Excel COM automation. Added `EXPORT_PDF=1`, tested export on `TG-R3922.xlsx` to create `TG-R3922.pdf`, attached the PDF in Telegram, compiled successfully, and restarted `Invoice Extractor Bot`.
