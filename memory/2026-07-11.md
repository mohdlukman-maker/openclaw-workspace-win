# 2026-07-11

## Shopee Affiliate Static Site Experiments

- Built first Netlify-ready static affiliate site for WOW Spaghetti instant pasta:
  `C:\Users\mohdl\.openclaw\workspace\projects\wow-spaghetti-affiliate-site`
- Built second Netlify-ready static affiliate site for a non-food product, iPhone shockproof case with camera lens protection:
  `C:\Users\mohdl\.openclaw\workspace\projects\iphone-lens-case-affiliate-site`
- Both sites use Bahasa Melayu product-promo copy, subtle footer affiliate disclosure, conservative claims, and Shopee affiliate URL constants near the bottom of `index.html`.
- Both sites use real Shopee-hosted product images saved under `assets/product/`, with source manifests in `assets/product/sources.json`.
- Both sites include a right-to-left auto carousel with manual left/right controls.
- Shopee review/rating endpoints were blocked by traffic verification/403, so exact buyer quotes were not used. Pages use safe positive feedback themes instead of fabricated testimonials.
- Next step when resuming: replace each `AFFILIATE_URL` in `index.html` with Lukman's generated Shopee affiliate link, then upload the chosen site folder to Netlify.
- Added Netlify single-site deployment automation:
  `C:\Users\mohdl\.openclaw\workspace\scripts\deploy-affiliate-netlify.ps1`
- Deployment guide:
  `C:\Users\mohdl\.openclaw\workspace\projects\README-affiliate-netlify.md`
- The deploy script accepts `-Product iphone` or `-Product spaghetti`, checks that the selected folder has exactly one HTML file, and deploys to the same Netlify site ID so the public URL remains the same.
- Netlify site was created via API:
  `lukman-affiliate-promo`
- Production URL to reuse every time:
  `https://lukman-affiliate-promo.netlify.app`
- Site ID is saved locally in ignored file:
  `C:\Users\mohdl\.openclaw\workspace\scripts\netlify-site.local.json`
- A draft deploy of the iPhone site succeeded:
  `https://6a51bdda00bdb4069a185592--lukman-affiliate-promo.netlify.app`
- Netlify token was used process-only and was not written into workspace files. Recommend Lukman rotates the token because it was pasted into chat.
- Production deploy completed for the iPhone case site with blank affiliate links:
  `https://lukman-affiliate-promo.netlify.app`
- Production deploy ID:
  `6a51c97a456c2d1ee1a58f38`
- Live HTML was checked with cache busting; it contains no Shopee URL, `AFFILIATE_URL` is blank, and four CTA links use `href="#"`.
- Built a local admin UI for the affiliate workflow:
  `C:\Users\mohdl\.openclaw\workspace\projects\affiliate-builder-ui`
- Start command:
  `node C:\Users\mohdl\.openclaw\workspace\projects\affiliate-builder-ui\server.js`
- UI URL:
  `http://localhost:8787`
- Features: category + period search (`day`, `week`, `month`), manual product URL paste, product title input, affiliate link input, generate HTML, iframe preview, and production publish to the existing Netlify site.
- Generated output path:
  `C:\Users\mohdl\.openclaw\workspace\projects\affiliate-builder-ui\generated\site`
- Publishing from UI requires the local server to start with `NETLIFY_AUTH_TOKEN` in the environment; token is not stored in files.
- Validation done: `/api/status` works, home UI returns 200, `/api/search` returned Shopee results for `iphone case`, `/api/generate` produced a preview with 4 images, `/preview/` returned 200, and `/api/publish` gives a clear missing-token error when the server lacks `NETLIFY_AUTH_TOKEN`.
- Lukman confirmed the Affiliate Builder UI works, but wants improvements later.
- Current generated preview output exists under:
  `C:\Users\mohdl\.openclaw\workspace\projects\affiliate-builder-ui\generated\site`
- When resuming, start with improvement ideas for the Affiliate Builder UI, likely around better product search quality, richer product extraction, cleaner generated templates, safer affiliate-link handling, and smoother Netlify publish flow.
- Wrap-up status before laptop shutdown: files are saved locally; no need to recreate the UI from scratch.
