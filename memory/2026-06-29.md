# 2026-06-29

- Improved `invoice_extractor` after Lukman approved the recommended hardening fixes. Added `/status` for bot/config/pending/latest-save checks, `/last` to resend the newest Excel/PDF pair, startup self-check logging, extraction review warnings, safer PDF export that keeps Excel saved when PDF fails, and `data/invoice_register.csv` logging for successful saves. Set `TELEGRAM_ALLOWED_CHAT_ID=5037627395` so only Lukman's direct Telegram chat can use the invoice bot. `py_compile` passed, startup checks passed, and the Windows scheduled task `Invoice Extractor Bot` was restarted successfully.
- Changed invoice output filenames to Lukman's requested PO format: `BFE PO TUJU GALAXY <CURRENT MONTH> <RUNNING NUMBER>`, for example `BFE PO TUJU GALAXY JUNE 0001.xlsx`; the running number is selected from existing monthly files plus `invoice_register.csv` and resets each month. Updated README, verified filename generation, compiled successfully, and restarted `Invoice Extractor Bot`.
- Updated `invoice_extractor` template fill behavior so the purchase-order workbook writes the generated output filename stem into PR Number (`J15`) and copies the extracted invoice number into Delivery Order No (`J18`). Verified with a temporary workbook (`J15=BFE PO TUJU GALAXY JUNE 0007`, `J18=TG-K08778`), compiled successfully, updated README, and restarted `Invoice Extractor Bot`.
- Updated `invoice_extractor` to support multiple authorized Telegram chats via `TELEGRAM_ALLOWED_CHAT_IDS`. Added Lukman's chat `5037627395` and the new approved user chat `5968727988`, added logging for `/start` and `/whoami`, added submitter chat/name fields to `invoice_register.csv`, kept backward compatibility with the old single-chat env var, compiled successfully, passed startup checks, and restarted `Invoice Extractor Bot`.
- Shortened new `invoice_extractor` PO output naming from `BFE PO TUJU GALAXY <MONTH> <RUNNING>` to `BFE PO TUJU <MONTH> <RUNNING>`, which also changes PR Number (`J15`) because it is filled from the output filename stem. Added `quantity_unit` extraction for item rows so units beside quantity such as `pcs` or `boxes` are captured, shown in review, and written into the template quantity cell (`G26:G54`) as values like `5 pcs`. Verified OCR parsing, filename generation, template cell output, compiled successfully, and restarted `Invoice Extractor Bot`.
- Updated `invoice_extractor` save flow so each sender's generated Excel/PDF files are stored under `data/invoices/<sender_chat_id>/` and copies are automatically sent back to the original Telegram sender after `/save` or delayed auto-save. `/last` now looks in the requester sender folder. Verified sender-folder path generation, compiled successfully, and restarted `Invoice Extractor Bot`.
- Fixed a false row-count reconciliation issue in `invoice_extractor`: OCR was reading numbered footer notes/payment instructions as invoice item rows, causing blank rows 4 and 5 in the review for invoice `TG-K08849`. Row-count detection now only scans lines inside the product table, reconciliation prompts explicitly ignore footer notes, blank placeholder line items are dropped, OCR quantity units tolerate separators like `10\ card`, common OCR unit alias `pes` maps to `pcs`, duplicate OCR rows from full-page/table-crop passes are deduped, `py_compile` passed, and the scheduled bot was restarted.
- Added a TUJU-specific focused extraction profile after Lukman suggested OCR should focus on the marked invoice area. The bot now detects TUJU invoices from focused vendor/details crops, then OCRs only the fixed vendor/header, invoice details, product table, and total zones before using full-page fallback for non-TUJU or weak cases. AI extraction/reconciliation also receives TUJU focused crops first. Verified the `TG-K08849` test image now extracts 3 rows (`4 roll`, `10 card`, `1 box`) with date `2026-06-20`, compiled successfully, startup checks passed, and `Invoice Extractor Bot` was restarted.
- Reset `invoice_extractor` saved PO outputs at Lukman's request by moving existing Excel/PDF files and the old `invoice_register.csv` into `data/reset_archive/reset_20260629_140017/`, leaving `data/invoices/` empty so official PO running numbers restart at `0001`. Added default testing behavior for Lukman's Telegram chat (`5037627395`) via `TELEGRAM_TEST_CHAT_IDS`: his `/save` outputs use `TEST BFE PO TUJU <MONTH> <NNNN>` and record type `test`, while other users remain official `record` outputs under their own sender folders. Added `/saverecord` and `/approverecord` to force an official save when needed. Updated PO template date logic so `J16` uses invoice date minus 2 days and moves back one more day if that would be Sunday. Verified compile, startup checks, official next stem `BFE PO TUJU JUNE 0001`, test stem `TEST BFE PO TUJU JUNE 0001`, and template date `2026-06-23 -> 20/06/2026`.
# 2026-06-29

- Improved `invoice_extractor` after Lukman approved the recommended hardening fixes. Added `/status` for bot/config/pending/latest-save checks, `/last` to resend the newest Excel/PDF pair, startup self-check logging, extraction review warnings, safer PDF export that keeps Excel saved when PDF fails, and `data/invoice_register.csv` logging for successful saves. Set `TELEGRAM_ALLOWED_CHAT_ID=5037627395` so only Lukman's direct Telegram chat can use the invoice bot. `py_compile` passed, startup checks passed, and the Windows scheduled task `Invoice Extractor Bot` was restarted successfully.
- Changed invoice output filenames to Lukman's requested PO format: `BFE PO TUJU GALAXY <CURRENT MONTH> <RUNNING NUMBER>`, for example `BFE PO TUJU GALAXY JUNE 0001.xlsx`; the running number is selected from existing monthly files plus `invoice_register.csv` and resets each month. Updated README, verified filename generation, compiled successfully, and restarted `Invoice Extractor Bot`.
- Updated `invoice_extractor` template fill behavior so the purchase-order workbook writes the generated output filename stem into PR Number (`J15`) and copies the extracted invoice number into Delivery Order No (`J18`). Verified with a temporary workbook (`J15=BFE PO TUJU GALAXY JUNE 0007`, `J18=TG-K08778`), compiled successfully, updated README, and restarted `Invoice Extractor Bot`.
- Updated `invoice_extractor` to support multiple authorized Telegram chats via `TELEGRAM_ALLOWED_CHAT_IDS`. Added Lukman's chat `5037627395` and the new approved user chat `5968727988`, added logging for `/start` and `/whoami`, added submitter chat/name fields to `invoice_register.csv`, kept backward compatibility with the old single-chat env var, compiled successfully, passed startup checks, and restarted `Invoice Extractor Bot`.
- Shortened new `invoice_extractor` PO output naming from `BFE PO TUJU GALAXY <MONTH> <RUNNING>` to `BFE PO TUJU <MONTH> <RUNNING>`, which also changes PR Number (`J15`) because it is filled from the output filename stem. Added `quantity_unit` extraction for item rows so units beside quantity such as `pcs` or `boxes` are captured, shown in review, and written into the template quantity cell (`G26:G54`) as values like `5 pcs`. Verified OCR parsing, filename generation, template cell output, compiled successfully, and restarted `Invoice Extractor Bot`.
- Updated `invoice_extractor` save flow so each sender's generated Excel/PDF files are stored under `data/invoices/<sender_chat_id>/` and copies are automatically sent back to the original Telegram sender after `/save` or delayed auto-save. `/last` now looks in the requester sender folder. Verified sender-folder path generation, compiled successfully, and restarted `Invoice Extractor Bot`.
- Fixed a false row-count reconciliation issue in `invoice_extractor`: OCR was reading numbered footer notes/payment instructions as invoice item rows, causing blank rows 4 and 5 in the review for invoice `TG-K08849`. Row-count detection now only scans lines inside the product table, reconciliation prompts explicitly ignore footer notes, blank placeholder line items are dropped, OCR quantity units tolerate separators like `10\ card`, common OCR unit alias `pes` maps to `pcs`, duplicate OCR rows from full-page/table-crop passes are deduped, `py_compile` passed, and the scheduled bot was restarted.
- Added a TUJU-specific focused extraction profile after Lukman suggested OCR should focus on the marked invoice area. The bot now detects TUJU invoices from focused vendor/details crops, then OCRs only the fixed vendor/header, invoice details, product table, and total zones before using full-page fallback for non-TUJU or weak cases. AI extraction/reconciliation also receives TUJU focused crops first. Verified the `TG-K08849` test image now extracts 3 rows (`4 roll`, `10 card`, `1 box`) with date `2026-06-20`, compiled successfully, startup checks passed, and `Invoice Extractor Bot` was restarted.
- Operational note for future `invoice_extractor` work: during restarts, check for duplicate Python bot processes because two polling instances have appeared before and can cause Telegram polling conflicts. Keep one clean scheduled-task instance of `Invoice Extractor Bot` running after changes.
- Updated `invoice_extractor` from invoice-first extraction to TUJU Delivery Order-first extraction. For TUJU D.O images, focused crop zones now target vendor/header, top-right D.O number/date, contact-person area, and product table; footer/stamp/signature zones are ignored unless fallback is needed. The D.O number is used for both Invoice No (`J17`) and Delivery Order No (`J18`), contact person/name/phone is written to `H59`, and PO date (`J16`) is now D.O date minus 4 days with Sunday moved back to Saturday. D.O item rows without price/amount columns are accepted, preserving quantity units like `4 roll`, `10 card`, `1 box`. Verified parser/template test with `TG-K08849`, contact `Farah 011-54302725`, `2026-06-20 -> 16/06/2026`, `py_compile` passed, README updated, and the scheduled bot was restarted.
- Updated \\invoice_extractor\\ to require a paired TUJU Delivery Order + Tax Invoice workflow before creating a P.O. The bot now stores the first extracted document, asks for the matching D.O/invoice, classifies document type, compares D.O number vs invoice number, item rows, quantities, and extra/missing rows, then merges the P.O payload using D.O date/contact/items and invoice unit price/amount where rows match. /review shows pair summary and pair warnings; /save is blocked until both documents are present. Verified py_compile, pair merge logic, template write to J16/J17/J18/H59 and item price/amount fields, updated README, and restarted the Windows scheduled task cleanly at 2026-06-29 14:42 Asia/Kuala_Lumpur.
- Added Material Requisition generation to \\invoice_extractor\\ after Lukman uploaded \\Material_Requsition.xlsx\\. Stored the template as \\invoice_extractor/data/templates/material_requisition_template.xlsx\\. After a matched D.O + invoice pair is saved, the bot now creates \\<PO stem> - MR.xlsx\\ and \\<PO stem> - MR.pdf\\ in the same sender folder and sends them back with the P.O Excel/PDF. MR reference \\N10\\ is the same P.O filename stem, date request \\N12\\ is P.O date minus 2 days, requested by \\D15\\ is the D.O contact person, and item rows fill \\B/C/K/L/N19:N40\\. Used Excel COM for MR creation because openpyxl-saved copies of this template could not be reopened by Excel for PDF export. Verified compile, MR Excel write, MR PDF export, startup check, and restarted \\Invoice Extractor Bot\\ at 2026-06-29 15:04 Asia/Kuala_Lumpur.
- Updated \\invoice_extractor/data/templates/material_requisition_template.xlsx\\ with Lukman's revised MR template containing the Assistant Project Manager signature/name. Adjusted MR generation so bottom-left REQUEST BY name at \\D52\\ is filled with the same D.O contact person used at top REQUESTED BY \\D15\\. Verified py_compile, MR Excel fill keeps 3 images/signatures and \\I53=Mohd Lukman Ali\\, MR PDF export passed, startup check passed, and restarted \\Invoice Extractor Bot\\ at 2026-06-29 15:19 Asia/Kuala_Lumpur.
- Tightened MR requested-by mapping in \\invoice_extractor\\: after D.O + invoice merge, \\delivery_order_contact_person\\ is explicitly copied from the D.O only, \\contact_person\\ is set to that D.O value, P.O \\H59\\ uses it, and MR \\D15\\ plus bottom-left \\D52\\ use it. Verified with a test where the invoice had a different contact; generated MR still wrote Farah 011-54302725 to both \\D15\\ and \\D52\\. Compile/startup checks passed and \\Invoice Extractor Bot\\ restarted at 2026-06-29 15:54 Asia/Kuala_Lumpur.
- Added procurement packet filesystem output to \\invoice_extractor\\. After saving a matched D.O + invoice pair, the bot now creates \\data/PROCUREMENT/TUJU GALAXY/<invoice-or-DO-number>/\\ and writes/copies four PDFs named \\1. MR.pdf\\, \\2. PO.pdf\\, \\3. Invoice.pdf\\, and \\4. D.O.pdf\\. P.O/MR PDFs are copied from generated outputs; Invoice and D.O PDFs are converted from the original Telegram images stored in the pending pair. Added procurement folder/status to \\/status\\ and register columns for procurement folder/files/issues. Verified py_compile, startup check, local procurement bundle test with sample image conversion, README update, and restarted \\Invoice Extractor Bot\\ at 2026-06-29 16:02 Asia/Kuala_Lumpur.
- Applied Lukman's requested invoice_extractor tidy fixes 1-3. Procurement packets are now versioned under \\data/PROCUREMENT/TUJU GALAXY/<invoice-number>/<PO-stem>/\\ to prevent repeated tests or saves from overwriting older packets. Added disk-backed pending pair persistence in \\pending_store.py\\, storing pending D.O + invoice review state under \\data/pending/<chat_id>.json\\ and clearing it after save/cancel. Split procurement packet logic into \\procurement.py\\ and pending persistence into \\pending_store.py\\ while leaving OCR/Excel code in \\invoice_bot.py\\ for lower-risk refactoring. Verified py_compile for all three modules, pending-store save/load/clear test, procurement wrapper test with versioned path and four PDFs, startup check, README update, and restarted \\Invoice Extractor Bot\\ at 2026-06-29 17:12 Asia/Kuala_Lumpur.
