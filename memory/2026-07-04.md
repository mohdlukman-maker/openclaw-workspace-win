## Procurement Index Hardening

- Fixed the apparent duplicate `invoice_bot.py` process by changing the `Invoice Extractor Bot` scheduled task action from the receipt-extractor venv launcher to `C:\Python314\python.exe`.
- Restarted the scheduled task and confirmed only one `invoice_bot.py` Python process is running.
- Added `procurement_normalize.py` to normalize procurement item and supplier aliases with an automatic SQLite backup before applying changes.
- Applied normalization to `invoice_extractor\data\procurement_index.sqlite`; backup saved under `invoice_extractor\data\backups\`.
- Item groups reduced from 1,579 to 1,559; item aliases increased to 3,043; suppliers reduced from 15 to 14; supplier aliases total 15.
- CMS PLC Cement variants now group as `CMS PLC Cement, 50kg/bag` with quantity 13,980.
- Meng Soon Huat variants now group under `MENG SOON HUAT ELECTRICAL SDN BHD`.
- Updated analytics/query code to use normalized item IDs and refreshed dashboard at `http://127.0.0.1:8790/`.
- Verification: compile checks passed and `python -m unittest discover -s tests` passed 20 tests.

## Procurement Dashboard Item Lookup

- Removed the visible "Ask Jarvis" panel from the local procurement dashboard.
- Added an item lookup search box: typing a prefix calls `/api/items?q=...` and lists normalized item suggestions.
- Selecting an item calls `/api/item-detail?id=...` and shows total quantity bought, total spend, average/min/max unit price, top 3 contacts by quantity, and recent unit price records.
- Refreshed dashboard at `http://127.0.0.1:8790/`.
- Verification: `/api/items?q=CMS` returns `CMS PLC Cement, 50kg/bag`; item detail shows quantity 13,980 and top contact Zarin.
- Updated item lookup to match words anywhere in the item name, so `Wash coarse sand` appears when searching `sand` or `coarse`.
