# 2026-06-26

- Created `invoice_extractor` beside `receipt_extractor` for Lukman's new invoice-document flow.
- The invoice bot accepts Telegram invoice photos/image documents, sends them to OpenAI vision, extracts structured invoice fields and line items, and writes to `invoice_extractor/data/invoices.xlsx`.
- Workbook sheets: `Invoices`, `LineItems`, and `Status`; `Status.document_status` starts as `pending` for a later fixed-format document generator.
- Verified `invoice_extractor/invoice_bot.py` with `py_compile` and initialized the workbook using the existing receipt bot virtual environment.
- Improved invoice line-item extraction after Lukman noticed missed rows. The bot now creates an enhanced table crop, sends both the full invoice and crop to OpenAI, stores raw extraction JSON, and records visible item numbers in `LineItems.item_no`.
- Tested against `I20260626074152348576.jpg`: two-image extraction with `gpt-4o-mini` captured all 14 visible rows correctly. Restarted the Telegram invoice bot; latest process was `9716`.
- Added a Telegram review gate because Lukman reported extraction was still incomplete. The invoice bot now replies with extracted item numbers/descriptions first and waits for `/save` or `/approve` before writing to Excel; `/review` repeats the list and `/cancel` discards the pending extraction.
- Hardened Excel-open handling: if `invoices.xlsx` is locked during `/save`, the bot keeps the reviewed extraction pending, asks Lukman to close Excel, and retries automatically for about 15 minutes instead of requiring a new photo.
- Changed invoice Excel output to one vertical `Invoices` sheet with only `Tax Invoice`, `Date`, `Item No`, `Description`, `Quantity`, `Unit Price`, and `Amount`. Added duplicate detection by Tax Invoice number: first `/save` warns, second `/save` saves only additional/new item rows when present, and `/saveall` forces all extracted rows.
- Added hybrid invoice extraction: the Telegram bot now tries local Tesseract OCR first, accepts it only when required fields and confidence pass thresholds, and falls back to OpenAI vision otherwise. Added `local_ocr_test.py` for tuning OCR against saved invoice photos. Installed Tesseract OCR `5.5.0.20241111`, set `TESSERACT_CMD` in `invoice_extractor/.env`, tuned local OCR to use the full image plus table crop, and verified the saved test invoice passes local OCR with confidence `58.0`.
- Fixed duplicate detection after OCR/AI variation: duplicate matching now normalizes invoice numbers (`O` vs `0`) and treats existing item numbers as repeated even when OCR reads a description, quantity, or amount slightly differently. Additional rows are detected by unseen item numbers.
