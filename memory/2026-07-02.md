# 2026-07-02

- Changed Jarvis/OpenClaw Telegram bot token to the new `@Jarvis_The_Meddling_Bot` bot, restarted `OpenClaw Gateway`, and verified Telegram delivery to Lukman's allowed private chat `5037627395`. Did not store the token value.
- Completed Phase 1 for `invoice_extractor`: automatic OpenAI fallback is disabled in runtime extraction, startup checks no longer require OpenAI auth, TUJU D.O number OCR now normalizes `TG-KO8849` to `TG-K08849`, noisy TUJU item rows now parse all three rows locally, and D.O review warnings no longer require unit price/amount. Verified with 10 unit tests plus saved D.O/invoice images; did not restart the scheduled Telegram bot yet.
- Restarted `Invoice Extractor Bot` at Lukman's request so Phase 1 code is active. Fresh log showed startup checks passed and Telegram application started. Phase 2 remains on hold. Noted that `data/pending/5037627395.json` still contains the stale pre-fix two-row D.O extraction and should be cleared/reprocessed before pairing with an invoice.
- Completed and restarted Phase 1.1 hardening for `invoice_extractor`: cleaned noisy contact OCR (`I: Fax: Farah` -> `Farah 011-54302725`), changed low-confidence wording to require careful review, added invoice document-total parsing/checks, kept D.O price warnings suppressed, and confirmed final P.O merge already prefers D.O contact. Verified with 13 unit tests plus latest saved D.O/invoice images. Phase 2 remains on hold.
- Completed and restarted Phase 1.2 focused ROI OCR for `invoice_extractor`: TUJU invoice images now use fixed header/contact/table/total crop zones (`tuju_invoice_focused`) instead of falling back to full-page OCR, while TUJU D.O keeps `tuju_delivery_order_focused`. Verified latest D.O and invoice still extract `TG-K08849`, `2026-06-20`, correct contacts, 3 rows, and invoice total `168.70`; 13 unit tests and syntax compile passed. Phase 2 remains on hold.
- Completed and restarted Phase 1.3 OCR efficiency pass for `invoice_extractor`: added per-document OCR cache, per-zone OCR timing/confidence/character-count logs, and zone-specific Tesseract config hooks while preserving current default OCR settings. Verified latest TUJU D.O/invoice outputs stayed correct and 13 unit tests/syntax compile passed. Phase 2 remains on hold.
- Completed and restarted Phase 1.4 direct routing for `invoice_extractor`: replaced D.O-first probing with one shared TUJU classifier crop that routes to D.O or invoice focused OCR, reducing expected invoice OCR calls from 6 to about 4 and D.O from 4 to about 3. Hardened TUJU number normalization for OCR `B` -> `8` and made Tax Invoice number take priority over `Our D/O No` in invoice headers. Verified latest samples keep correct fields; D.O confidence remains low but all fields/rows parse.
- Completed and restarted Phase 2 for `invoice_extractor`: added explicit known document profiles (`tuju_delivery_order`, `tuju_invoice`), routed extraction through profile matching, stored `document_profile` in extracted data, and added clean unknown-format handling that tells Lukman registration is not available yet instead of forcing unknown documents through TUJU OCR. Verified 16 unit tests, syntax compile, and latest TUJU samples.
# 2026-07-02

- Changed Jarvis/OpenClaw Telegram bot token to the new `@Jarvis_The_Meddling_Bot` bot, restarted `OpenClaw Gateway`, and verified Telegram delivery to Lukman's allowed private chat `5037627395`. Did not store the token value.
- Completed Phase 1 for `invoice_extractor`: automatic OpenAI fallback is disabled in runtime extraction, startup checks no longer require OpenAI auth, TUJU D.O number OCR now normalizes `TG-KO8849` to `TG-K08849`, noisy TUJU item rows now parse all three rows locally, and D.O review warnings no longer require unit price/amount. Verified with 10 unit tests plus saved D.O/invoice images; did not restart the scheduled Telegram bot yet.
- Restarted `Invoice Extractor Bot` at Lukman's request so Phase 1 code is active. Fresh log showed startup checks passed and Telegram application started. Phase 2 remains on hold. Noted that `data/pending/5037627395.json` still contains the stale pre-fix two-row D.O extraction and should be cleared/reprocessed before pairing with an invoice.
- Completed and restarted Phase 1.1 hardening for `invoice_extractor`: cleaned noisy contact OCR (`I: Fax: Farah` -> `Farah 011-54302725`), changed low-confidence wording to require careful review, added invoice document-total parsing/checks, kept D.O price warnings suppressed, and confirmed final P.O merge already prefers D.O contact. Verified with 13 unit tests plus latest saved D.O/invoice images. Phase 2 remains on hold.
- Completed and restarted Phase 1.2 focused ROI OCR for `invoice_extractor`: TUJU invoice images now use fixed header/contact/table/total crop zones (`tuju_invoice_focused`) instead of falling back to full-page OCR, while TUJU D.O keeps `tuju_delivery_order_focused`. Verified latest D.O and invoice still extract `TG-K08849`, `2026-06-20`, correct contacts, 3 rows, and invoice total `168.70`; 13 unit tests and syntax compile passed. Phase 2 remains on hold.
- Completed and restarted Phase 1.3 OCR efficiency pass for `invoice_extractor`: added per-document OCR cache, per-zone OCR timing/confidence/character-count logs, and zone-specific Tesseract config hooks while preserving current default OCR settings. Verified latest TUJU D.O/invoice outputs stayed correct and 13 unit tests/syntax compile passed. Phase 2 remains on hold.
- Completed and restarted Phase 1.4 direct routing for `invoice_extractor`: replaced D.O-first probing with one shared TUJU classifier crop that routes to D.O or invoice focused OCR, reducing expected invoice OCR calls from 6 to about 4 and D.O from 4 to about 3. Hardened TUJU number normalization for OCR `B` -> `8` and made Tax Invoice number take priority over `Our D/O No` in invoice headers. Verified latest samples keep correct fields; D.O confidence remains low but all fields/rows parse.
- Current `invoice_extractor` direction from Lukman: keep Phase 2 document-profile/new-format registration on hold until local OCR Phase 1 optimizations are solid. AI must remain disabled for runtime OCR; future AI use should be considered only for optional registration/profile-building after local OCR is reliable.
- Next useful action after Lukman retests Phase 1.4: inspect the new OCR timing logs and confirm OCR call counts dropped as expected. If still slow, tune zone-specific Tesseract configs before considering parallel OCR.
